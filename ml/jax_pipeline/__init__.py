"""JAX/Flax research pipeline for MedExtract.

For benchmarking and research comparison against the PyTorch and TensorFlow
pipelines — not production. Public entry point:
`jax_pipeline.inference.extract(text)`.
"""
